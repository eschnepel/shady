[mypy]
python_version = 3.14
strict = True
ignore_missing_imports = True
no_namespace_packages = True
warn_unused_ignores = True
mypy_path = .

# Home Assistant base classes are typed as Any when stubs are absent.
# warn_unused_ignores = False suppresses the cascade of "unused type: ignore"
# errors that result from @callback and HA base-class decorators being untyped.
# Module names are resolved as "shady.*" because custom_components/ is a
# namespace package (no __init__.py, as required by Home Assistant).
# See ADR-000 §2 for the full rationale behind this per-file suppression
# approach (never a global disable_error_code).
[mypy-shady.config_flow]
warn_unused_ignores = False

[mypy-shady.sensor]
warn_unused_ignores = False

[mypy-shady.coordinator]
warn_unused_ignores = False

# select.py (ShadyDiagnosticModeSelect, ADR-004 §2a), button.py
# (ShadyRecalculateButton, ADR-002 §4; ShadyClearDiagnosticSlotButton,
# ADR-004 §2f), and datetime.py (ShadyDiagnosticSlotDateTime, ADR-004
# §2f) each subclass an HA entity the same way sensor.py does.
[mypy-shady.select]
warn_unused_ignores = False

[mypy-shady.button]
warn_unused_ignores = False

[mypy-shady.datetime]
warn_unused_ignores = False
