# Task Index

**This is a new planning cycle**, per `tasks/archived/INDEX.md`'s own closing
note ("Any further work on this project — new features, new ADRs, a fresh audit
pass — starts a new planning cycle from Phase 0/1 rather than continuing this
remediation batch"). The 20 original capability tasks and their 13 round-1 and 4
round-2 remediation/audit follow-ups all live in `tasks/archived/` and
`tasks/AUDIT-*.md`/`tasks/AUDIT-INDEX.md` — nothing there is reopened or
renumbered by this cycle.

This cycle was triggered directly by an uploaded bug report
(`shady-baseline-history-fix.md`, itself the output of a prior analysis session)
rather than by a fresh Phase 1 capability-slicing pass — a single, well-scoped,
already-root-caused fix rather than a new set of vertical slices. Phase 0's
amendment procedure was still followed in full (ADR-009 §1c, ADR-012 §2a,
`tasks/adr-summary.md` — see the Refinement Log below), and the task itself uses
the same `tasks/<slug>.md` template and Consumed Interfaces/Delivered Artifacts
contract as every prior cycle. Numbering continues unambiguously from the last
archived task (`TASK-0033`), per the orchestration prompt's Phase 6 numbering
rule.

## Task table

| Slug | Title | Status | Dependencies | Worker |
| -- | -- | -- | -- | -- |
| TASK-0034-baseline-recorder-backed-history | Recorder-Backed History for the `forecast_solar` Baseline (Fix Permanent Cold-Start Passthrough) | done | — | Lead Agent (inline; no sub-agents available in this environment — same convention `tasks/AUDIT-INDEX.md`'s round 2 already documents) |
| TASK-0034-patch-1 | Self-Heal a Frozen `history_entity_id` (Startup Retry, Not Just Discovery-Time) | done | TASK-0034-baseline-recorder-backed-history | Lead Agent (inline) |
| TASK-0034-patch-2 | Historical Backfill Silently Skipped for an Already-Push-Marked Baseline (Permanent Cold-Start Passthrough, Take 2) | done | TASK-0034-baseline-recorder-backed-history, TASK-0034-patch-1 | Lead Agent (inline) |
| TASK-0035-reconfigure-flow-and-string-schema | Config Flow Redesign — Paged Global Settings, Multi-Select Strings, Per-String Settings Loop, Reconfigure-Flow Migration | done | — | Lead Agent (inline) |
| TASK-0036-service-call-response-cache | Route Every Outbound Service Call Through a Persisted Last-Good-Response Cache | done | — | Lead Agent (inline) |
| TASK-0037-pinned-slot-pool-not-yet-elapsed-freeze | `get_pinned_slot_pool` Permanently Freezing a Not-Yet-Elapsed Slot at `None` (Diagnostic "selected" Series Missing) | done | TASK-0015b-diagnostics-select-and-scatter-sensors | Lead Agent (inline) |
| TASK-0037-patch-1-ridge-regularization-and-exception-isolation | `wls2`/`wls3` Genuinely Raising `LinAlgError: Singular Matrix` for Real-Scale, Low-Variety `FC` (Diagnostic "selected" Series Still Missing After `TASK-0037`) | done | TASK-0037-pinned-slot-pool-not-yet-elapsed-freeze | Lead Agent (inline) |
| TASK-0037-patch-2-allow-historical-backfill-for-get-time-range | A Push-Sourced (`forecast_solar`-Shaped) Baseline's Already-Elapsed History Was Never Fetched At All (Diagnostic "selected" Series/Accuracy Still Missing After `TASK-0037-patch-1`) | done | TASK-0037-patch-1-ridge-regularization-and-exception-isolation | Lead Agent (inline) |
| TASK-0037-patch-3-forward-fill-pushed-baseline-series | `_push_provider_series` Never Forward-Filled a Coarser-Than-5-Minute `forward()` Series (Diagnostic "selected" Series Only Ever Appeared On The Hour) | done | TASK-0037-patch-2-allow-historical-backfill-for-get-time-range | Lead Agent (inline) |
| TASK-0037-patch-4-follow-latest-diagnostic-slot-toggle | Diagnosed-Slot Companion Button Replaced by an Auto-Follow Toggle (`ShadyClearDiagnosticSlotButton` → `ShadyFollowDiagnosticSlotSwitch`) | done | TASK-0037-patch-3-forward-fill-pushed-baseline-series | Lead Agent (inline) |
| TASK-0015b-patch-1-apexcharts-card-series-format | `series` Entries Reshaped for Direct `apexcharts-card` Use | done | TASK-0015b-diagnostics-select-and-scatter-sensors | Lead Agent (inline) |
| TASK-0015b-patch-2-plotly-graph-xy-series-format | `series` Entries Reshaped Again — `custom:plotly-graph` x/y Arrays, Not `apexcharts-card` | done | TASK-0015b-patch-1-apexcharts-card-series-format | Lead Agent (inline) |
| TASK-0015b-patch-3-xy-series-entry-on-base-class | `_xy_series_entry` Moved to `DiagnosticMode` — Shared by Every Diagnostic Mode | done | TASK-0015b-patch-2-plotly-graph-xy-series-format | Lead Agent (inline) |

## Refinement Log

| Date | Trigger task | Action | Reason |
| -- | -- | -- | -- |
| 2026-09-15 | TASK-0034-baseline-recorder-backed-history | Amended ADR-009 (new §1c) and ADR-012 (new §2a); updated `tasks/adr-summary.md` §2/§9 and `README.md`'s Requirements section | Phase 0's amendment procedure: the fix specified in `shady-baseline-history-fix.md` is a real architecture decision (a `forecast_solar` baseline candidate may now carry a linked, recorder-backed history entity, resolved via the entity registry; `coordinator.py`'s `_fetch_fn` dispatch gains a second recorder-backed branch), not a same-behavior refactor — it needed to be recorded as an ADR amendment before implementation, not discovered only from the diff. |
| 2026-09-15 | TASK-0034-baseline-recorder-backed-history | Created this fresh `tasks/INDEX.md` rather than reopening `tasks/archived/INDEX.md` | `tasks/archived/INDEX.md` closed itself out explicitly ("nothing remains `todo`... any further work... starts a new planning cycle"); reopening it for one new task would blur the closed remediation batch's own audit trail. |
| 2026-09-15 | TASK-0034-baseline-recorder-backed-history | Revised the design mid-task, before any code was written against the first version: `history_entity_id` moved from a `BaselineProvider`-specific field (with an `isinstance(provider, BaselineProvider)` check in `coordinator.py`'s `_fetch_fn`) to a fourth, generic, optional method on the `Provider` base class itself (`history_entity_id() -> str \| None`, default `None`), the same opt-in shape `identify()`/`forward()` already have. `_fetch_fn`'s dispatch, and the mirrored recorder-query method, were renamed accordingly (`_fetch_provider_history_statistics`, not `_fetch_baseline_statistics`). ADR-012 §1/§2a and ADR-009 §1c were revised in place to match before implementation resumed — not left describing a design the code no longer matched. | Human directive, given before implementation started: "The base class of all baseline providers should cover this scenario too... so another pv forecast or the weather forecast history part can be added later with less effort." A `BaselineProvider`-only field would have meant every future provider re-implementing its own `isinstance` branch in `_fetch_fn` — exactly the per-provider coordinator coupling ADR-012 §4's `forward()` design already rejected for the push path. |
| 2026-09-15 | TASK-0034-baseline-recorder-backed-history | Implemented (against the revised, generic design above), reviewed (Phase 4a/4b, inline — Lead Agent acting as Worker then Reviewer), and merged in full. All Acceptance Criteria verified against the delivered diff — **PASS**. `Status` moved `todo` → `in-progress` → `done`. Full suite green (`pytest`, 558 passed), `mypy --config-file mypy.ini custom_components/ tests/` clean (58 files), `ruff check .`/`ruff format --check .` clean (174 files), `mdformat --check` clean on every edited/created `.md` file. No new external dependency (`tasks/DEPENDENCIES.md` unchanged — the entity registry ships with the `homeassistant` package itself, same category as `aiohttp`/`homeassistant.helpers.device_registry`, not something this project installs). | Routine Phase 4c close-out. |
| 2026-09-15 | TASK-0034-baseline-recorder-backed-history | Corrected a factual assumption in the already-`done` resolution logic, same session, before any downstream task consumed it (no patch task created — see Reason): the human reported the real Forecast.Solar companion sensor's `entity_id` carries no config-entry-scoping prefix or suffix at all (it is exactly `sensor.power_production_now`), invalidating the composed-`unique_id` lookup (`f"{config_entry_id}_power_production_now"` via `async_get_entity_id`). `_resolve_forecast_solar_history_entity` (`providers/discovery.py`) was rewritten to match within `async_entries_for_config_entry(registry, config_entry_id)` on `domain == "sensor"` and `translation_key == "power_production_now"` instead — a lookup with no assumption about `entity_id`/`unique_id` shape at all. `tests/test_providers_discovery.py`'s fake entity-registry doubles were rewritten to model `translation_key` instead of `unique_id`, with two new resolution tests (rename-survives, wrong-`translation_key`-does-not-match). This round's own catch-up work (after a context-window handoff) then: renamed leftover `sensor.home_power_production_now` fixture strings to the correct unprefixed form across `test_coordinator.py`/`test_config_flow.py`/`test_providers_discovery.py`; rewrote ADR-009 §1c/§4 and ADR-012 §5 to describe the `translation_key` mechanism instead of the abandoned `unique_id` one; fixed a stale method-name reference (`_fetch_baseline_statistics` → `_fetch_provider_history_statistics`, the name actually delivered) found independently in ADR-012 §5 and `tasks/adr-summary.md` (two places) while doing that pass; updated `TASK-0034`'s own Acceptance Criteria/Delivered Artifacts to match. Full suite reverified green after every fix: `pytest` (560 passed — two more than the original 558, the two new resolution tests), `mypy --config-file mypy.ini custom_components/ tests/` clean (58 files), `ruff check .` clean (one `E501` line-length violation in the rewritten resolver, fixed by `ruff format`), `ruff format --check .` clean (174 files), `mdformat --check` clean on every edited `.md` file (two required a `mdformat` pass — pure line-reflow from the edits above, no content change, reverified after). | Golden rule for sub-agents applies to the Lead Agent too: a wrong assumption caught immediately, with zero downstream consumers yet, is corrected in place rather than guessed-around or left for a separate patch task — Phase 6 Scenario C's patch-task mechanism is for a `done` task's interface proving insufficient to a *downstream* worker after the fact, which had not yet happened here. Status stays `done` throughout; this row is the audit trail Scenario C would otherwise provide. |
| 2026-09-17 | TASK-0034-baseline-recorder-backed-history | Created `TASK-0034-patch-1` (Phase 6 Scenario C) as a proper patch task, dependency `[TASK-0034-baseline-recorder-backed-history]` — unlike the same-session `unique_id` → `translation_key` correction logged above, this is a genuinely different situation: a *downstream* user hit this after the original task had already shipped and been running for a while, discovered independently of any in-flight work. Root cause: `history_entity_id` resolution is one-shot, at config/options-flow submission time, and its result — including a `None` from losing a one-time startup race — gets persisted into config entry data and never retried. Fixed via a startup-time self-heal retry (`coordinator.py`'s `_resolve_stale_forecast_solar_history_entities`, called before `_backfill_elapsed_today_slots`), persisting a successful late resolution back into config entry data. Human's explicit choice: persist the self-heal (not in-memory-only), so storage/diagnostics reflect reality too, not just this session's runtime state. ADR-009 §1c received a further amendment documenting the retry; `tasks/adr-summary.md` updated to match. Implemented, reviewed, and verified inline (Lead Agent as Worker then Reviewer) — all Acceptance Criteria met, **PASS**. Full suite green: `pytest` 569 passed (560 + 9 new), `mypy --strict` clean (58 files), `ruff check`/`format --check` clean (174 files, after one `ruff format` pass on the new tests), `mdformat --check` clean. `Status` → `done`. | A `done` task's interface proving insufficient to a downstream consumer, discovered independently after the fact, is exactly Phase 6 Scenario C's own trigger — unlike the earlier same-session correction, this one gets the full patch-task treatment: original task's `Status` untouched, dependency link recorded, its own Delivered Artifacts block. |
| 2026-09-17 | TASK-0035-reconfigure-flow-and-string-schema | Created `TASK-0035-reconfigure-flow-and-string-schema` (new task, not a patch — independent of `TASK-0034`/`TASK-0034-patch-1`, touches `config_flow.py` only, which neither of those touched) and amended ADR-010 *before* writing the task file, per Phase 0's own amendment procedure — this is a real architecture decision (removing `ShadyOptionsFlow` entirely, adopting `ConfigFlow.async_step_reconfigure` + `async_update_reload_and_abort`), not a same-behavior refactor. Root cause (confirmed against Home Assistant's own documentation): `ShadyOptionsFlow`'s final step wrote to `entry.options` via `async_create_entry`, which `coordinator.py` never reads (`entry.data` only, ADR-002 §1a) — no reconfiguration has ever actually taken effect, since initial release; not a race, a standing bug. Human's explicit choice, given two real options (minimal patch: write `entry.data` directly + explicit reload vs. full modernization: migrate to `async_step_reconfigure`): the bigger modernization. Two independent, smaller corrections bundled into the same task because they touch the same schema function this migration already touches: `add_string`'s actual-yield entity field, specified in ADR-010's original text as an entity selector but never actually implemented that way (a plain text field instead) — corrected to match the ADR that already called for it, not a new decision; and the string `name` field becomes optional (human request). `tasks/adr-summary.md` §7 updated to match the amendment. `Status: todo` — implementation is the next step, not part of this entry. | New task admitted outside the normal Phase 1/2 capability-slicing flow, same pattern `TASK-0034` itself used for a directly-reported bug — Phase 1's own vertical-slice derivation only applies to a fresh planning cycle's initial capability set, not to every later addition. |
| 2026-09-17 | TASK-0035-reconfigure-flow-and-string-schema | Revised the design before any implementation started (same pattern as `TASK-0034`'s own mid-task design pivot, logged above — a design changing before it becomes code is exactly what ADRs-as-an-intermediate-step exist to make cheap). Human asked to study how Effy (`github.com/eschnepel/effy`) handles multiple sensors before the rewrite proceeded; its own multi-sensor fields turned out to be a single `multiple: true` entity selector (no per-item settings concept at all — every sensor treated identically), not directly reusable, but adopted as the mechanism for *which entities are strings* specifically, while each string's own settings (name, baseline override, temperature/clipping fields) still get a page of their own — something Effy has no equivalent of. An initial proposal to use Home Assistant's native `ConfigSubentries` for the per-string pages was raised and then dropped: subentries manage their own independent add/remove lifecycle, which doesn't fit a design where the string list is defined by an earlier step's multi-select rather than each subentry's own flow. Settled design instead: a `strings` step (multi-select) feeds a `string_settings_hub`/`string_settings_edit` loop — one shared edit-step method with "which entity_id is currently being edited" tracked in flow state, the exact pattern `homeassistant.helpers.schema_config_entry_flow` itself documents, dispatched via a `vol.In` dropdown rather than `async_show_menu` (menu dispatch needs a fixed, a-priori-known set of step names, which a dynamic per-string list can't provide). Global settings, previously one crowded "settings" step, split into three pages: `baseline` (moved first in step order, ahead of `strings`, per human's explicit ordering request), `regression_tuning`, `advanced_optional`. Reconfigure now linear-vs-menu split: `async_step_user` walks the full sequence in order (human's request); `async_step_reconfigure` opens on an `async_show_menu` of the same sections instead, each returning to that menu rather than proceeding onward — a genuine fit for `async_show_menu` since the section list, unlike the string list, is small and fixed. Removing a string from the multi-select discards its settings from the in-progress result (human's explicit choice — no orphan retention). No migration from the old `CONF_STRINGS` list-of-dicts shape to the new entity_id-keyed shape — human's explicit choice, exactly one installation exists. ADR-010's `TASK-0035` amendment and `tasks/adr-summary.md` §7 rewritten in place to describe this final design (the amendment now explicitly marks itself as revising the same-task, not-yet-implemented sketch it replaces, rather than presenting two competing unimplemented plans side by side); `tasks/TASK-0035-reconfigure-flow-and-string-schema.md` rewritten in full to match, including the Related ADRs list, Acceptance Criteria, and footprint. **Implementation explicitly not started this round** — human's own instruction: update/create the ADR and task files, stop there. `Status` remains `todo`. | Design changed before any code existed, same category as `TASK-0034`'s own "Revised the design mid-task" entry above — the cost of this revision is entirely in markdown, exactly the trade-off `tasks/*.md`-as-intermediate-step is meant to buy. |
| 2026-09-18 | TASK-0035-reconfigure-flow-and-string-schema | Integrated ADR-010's `TASK-0035` amendment into the ADR's main text, per explicit human instruction, before implementation began: rewrote ADR-010 so the reshaped flow (five steps, `baseline`/`strings`/`string_settings_hub`+`string_settings_edit`/`regression_tuning`/`advanced_optional`, plus `async_step_reconfigure`) is presented as *the* Decision, not a layer bolted onto the original three-step sketch it replaces — the original sketch's own fenced spec was removed rather than kept as a competing "historical record" block, since git history (and this Refinement Log) already preserve it; nothing about the design itself changed in this pass, only its presentation. `tasks/adr-summary.md` §6/§7 left as-is at this point (still correctly described the target design, still marked `Status: todo`) — updated only once implementation actually landed, see the entry below. | Human's explicit instruction, given as this round's first request, ahead of "work on task 35" — matches Phase 0's general principle that ground-truth documents should read as coherent current-state descriptions, not amendment-on-amendment layers, once a design has actually settled. |
| 2026-09-18 | TASK-0035-reconfigure-flow-and-string-schema | Implemented in full (Lead Agent inline, no sub-agents available in this environment), reviewed against every Acceptance Criterion (Phase 4a/4b, inline — Lead Agent as Worker then Reviewer) — **PASS**. `const.py`: removed `CONF_STRING_ACTUAL_YIELD_ENTITY`/`CONF_STRING_CONFIGURE_ADVANCED`, added the `STRING_SETTINGS_HUB_DONE` sentinel. `config_flow.py`: full rewrite to the five-step/menu-reconfigure shape described in ADR-010/§7 above; `ShadyOptionsFlow`/`async_get_options_flow` removed outright, not left as a stub. `coordinator.py`: `_resolve_string`/`_StringConfig` construction and the Forecast.Solar startup self-heal (`TASK-0034-patch-1`) updated in lock-step for `CONF_STRINGS`'s new `dict[entity_id, settings]` shape (was `list[dict]`) — no other production file touches `CONF_STRINGS` at all, confirmed by a full-codebase grep before starting, so `sensor.py`/`button.py`/`select.py` needed no changes (fully isolated by the `_StringConfig` dataclass). `translations/en.json`/`de.json` rewritten for the new step set (`options` section removed entirely; `config.abort.reconfigure_successful` added). `README.md`'s Configuration section rewritten to match. `tasks/adr-summary.md` §6/§7 updated to drop the "not yet implemented" framing now that the design is what's actually shipped. One real bug caught by the new tests during implementation, not a pre-existing one: `_optional_float`'s helper for `string_settings_edit`'s form defaults crashed on a stored `None` (rather than falling back to the schema's own blank-string sentinel) for a string that had never had its converter limit or rated DC capacity edited — `existing.get(key, "")` only falls back when a key is *absent*, not when present with value `None`; fixed via a new `_blank_if_none` helper, used for both affected fields. Full suite green: `pytest` 590 passed (569 baseline, minus the 28 old `test_config_flow.py`/`test_translations.py` cases the old three-step flow's tests, plus 42 new `test_config_flow.py` cases and 7 rewritten `test_translations.py` cases for the new shape — net +21), `mypy --config-file mypy.ini custom_components/ tests/` clean (58 files), `ruff check .`/`ruff format --check .` clean, `mdformat --check` clean on every edited/created `.md` file including `README.md`. No new external dependency — `tasks/DEPENDENCIES.md` unchanged (`selector.EntitySelector`/`EntitySelectorConfig` ship with the `homeassistant` package itself, same category as `aiohttp`). `Status` → `done`. | Routine Phase 4c close-out, immediately following the ADR-integration pass above in the same session. |
| 2026-09-19 | TASK-0036-service-call-response-cache | Created `TASK-0036` from a direct human request ("route any PV forecast/weather service call through the cache... so the cache can recall the last result if the current call fails... persisted"); wrote the task spec first for review, per explicit instruction, before any code changed. Human then redirected the design twice before implementation: (1) rejected a standalone `service_cache.py` module in favor of integrating the new store into the existing `cache.py` — no separate module gains anything `cache.py`'s own established non-time-series-store precedent (energy totals, ramp state, fitted models) doesn't already cover, and a second module would duplicate `cache.py`'s injected-callable/`coordinator.py`-owned-persistence invariants for no behavioral gain; (2) fixed the recall expiry at 12 hours (`SERVICE_RESPONSE_MAX_AGE`), rather than leaving it open. Amended ADR-000 §3 (new `providers --> cache` mermaid edge — `providers/discovery.py` imports `ServiceResponseCache`/`service_call_key` directly, the same narrow exception `diagnostics/compare_regressions.py`'s pre-existing `SLOTS_PER_DAY` import already established), ADR-007 (new §1a: the sixth, service-response store, restart-persisted via its own injected duck-typed store object rather than fully `coordinator.py`-mediated restore — necessary because the construction-time Forecast.Solar poll fires before any explicit restore step could run), ADR-012 §4b (fallback behavior), and ADR-009 §4 (discovery-time sampling now routes through the same shared cache) — all before implementation, per Phase 0. `tasks/adr-summary.md` §2/§5 updated to match. | Phase 0's amendment procedure: a new restart-persisted store, a new cross-module import edge, and a behavior change at two existing call sites are real architecture decisions, not a same-behavior refactor — recorded before the diff existed, not discovered from it. |
| 2026-09-19 | TASK-0036-service-call-response-cache | Implemented in full (Lead Agent inline, no sub-agents available in this environment), reviewed against every Acceptance Criterion (Phase 4a/4b, inline — Lead Agent as Worker then Reviewer) — **PASS**. `cache.py` gained `ServiceResponseCache`/`service_call_key`/`SERVICE_RESPONSE_MAX_AGE`/`ServiceResponseStore` exactly per the amended ADR-007 §1a design (injected duck-typed store, lazy load on first `async_call`, 12-hour inclusive expiry, JSON-safe persistence). `providers/discovery.py` gained the `hass.data` singleton accessor (`async_get_service_response_cache`) and routed `_sample_weather_forecast`/`_sample_forecast_solar` through it. `coordinator.py` wired a shared, domain-wide-keyed `Store` into the same singleton at `__init__` and routed `_poll_forecast_solar` through it, replacing the old unconditional `provider.update_live_forecast(response)` with "only push a genuinely usable (live or recalled) response" — a small, tightly-coupled behavior improvement beyond the letter of the bug report (a cold, `wh_period`-less response is no longer pushed into the provider either, not just an aged-out recall). Adding `providers/discovery.py`'s new `..cache`/`..const` imports broke the file-path-load ordering convention in 8 existing test files (each `_load`s `providers/discovery.py` before `cache.py`/`const.py`/`regression/base.py`) — fixed mechanically (reordering only, no logic changes) rather than worked around, since the alternative (avoiding the import) would have undone the human's own two directives. Full suite green: `pytest` 630 passed (591 baseline + 39 new: 28 in new `test_cache_service_responses.py`, 6 in `test_coordinator_provider_push.py`, 5 in `test_providers_discovery.py`), `mypy --config-file mypy.ini custom_components/ tests/` clean (59 files), `ruff check .`/`ruff format --check .` clean (178 files), `mdformat --check` clean on every edited/created `.md` file. No new external dependency (`tasks/DEPENDENCIES.md` unchanged). `Status` → `done`. | Routine Phase 4c close-out, immediately following the Phase 0 amendment pass above in the same session. |
| 2026-09-20 | TASK-0034-baseline-recorder-backed-history | Created `TASK-0034-patch-2` (Phase 6 Scenario C) — same pattern as `TASK-0034-patch-1`: a `done` task's delivered interface proving insufficient to a downstream consumer, discovered independently after the fact, not mid-flight work. Reported live, in chat, by a user running the `initialcode` branch: every string's forecast sensor showed the identical, raw, un-corrected value — `TASK-0034`'s own original symptom, recurring despite both `TASK-0034` and `TASK-0034-patch-1` already being `done` and the recorder genuinely holding 60 days of real history. Root-caused live (no uploaded bug report this time — direct interactive diagnosis against the user's running instance, including two rounds of temporary, hand-written diagnostic logging deployed to and removed from their own deployment, per their explicit request) to a cache-layer defect, not a resolution or recorder-eligibility problem: `_validate_range`'s pre-existing `to_index is None` ("actively pushed by Shady" — ADR-007a §2, older than and unrelated to `TASK-0034`) short-circuited *before* checking whether `get_regression_pools`'s requested historical window even needed fetching — so the historical backfill `TASK-0034` added never ran at all for any baseline that had already received a live push (which is every `forecast_solar`-shaped baseline, on every startup, before any refit ever fires). A first fix attempt (removing the short-circuit's `return` unconditionally) was caught by the project's own regression suite (`TestPushGuardAndPushOnlySensor`, `tests/test_cache_core.py`) before being finalized — it would have made *every* push-marked sensor queryable, including Shady's own purely-computed output series with no external source at all. Corrected to an opt-in `allow_historical_backfill` parameter on `_validate_range`, defaulting to `False` (every existing call site unchanged), set only by `get_regression_pools`. A second, related defect was found and fixed in the same pass, before it could ship: `_fetch_and_store`'s `_validated`-merge logic silently replaced a `to_index=None` marker with a real boundary the first time it was called for a push-marked sensor (previously unreachable code, now reachable via the new opt-in path) — left unfixed, this would have let a later live read re-fetch and overwrite an already-pushed future value. Fix confirmed live against the reporting user's own instance before this task file was written: the baseline regression pool went from `0/24192` non-`NaN` cells to `23915/24192`, matching the actual-yield side's own coverage. No ADR amendment — both ADR-007a §2 and ADR-012 §2a already correctly describe the intended behavior; the two ADR-described contracts simply defeated each other in the original implementation, which this task corrects without changing either decision. Implemented, reviewed, and verified inline (Lead Agent as Worker then Reviewer) — all Acceptance Criteria met, **PASS**. Full suite green: `pytest` 635 passed (630 + 5 new), `mypy --config-file mypy.ini custom_components/ tests/` clean (59 files), `ruff check .`/`ruff format --check .` clean (178 files), `mdformat --check` clean. `Status` → `done`. | A `done` task's interface proving insufficient to a downstream consumer, discovered independently after the fact — Phase 6 Scenario C's own trigger, same as `TASK-0034-patch-1`. Original task's `Status` left untouched throughout; dependency link recorded on both prior `TASK-0034` rounds; this row plus the task file's own Goal section are the audit trail. |
| 2026-09-21 | TASK-0015b-diagnostics-select-and-scatter-sensors | Created `TASK-0015b-patch-1` (Phase 6 Scenario C) from a direct human request against a `done` task's already-shipped interface: `ShadyDiagnosticsSensor`'s `series` attribute (ADR-004 §2) was shaped for the raw ApexCharts JS library (`{"name": ..., "data": [[x, y], ...]}`), not for `apexcharts-card`, the Lovelace card most installs actually use to render it — the human confirmed, live against a running instance, that `{"entity": ..., "name": ..., "data_generator": "return [[x, y], ...]"}` is what that card actually expects per series block. One design fork needed resolving before implementation — replace the old shape outright, or keep both — put to the human directly rather than guessed: **replace entirely**, their explicit choice (one format this attribute emits, matching its one purpose). Amended ADR-004 (new §2c documenting the shape/rationale, and a §5 amendment covering the one narrow reshaping step this moved into `sensor.py` — `entity` is the one key `diagnostics/` cannot supply itself, since it has no notion of its own future `entity_id`) *before* implementation, per Phase 0; `tasks/adr-summary.md` §6 updated to match. | Golden rule: a breaking format change to an already-`done`, ADR-documented, test-pinned contract is a real architecture decision, not a same-behavior refactor — put to the human as a concrete fork rather than assumed, and recorded as an ADR amendment before any diff existed. |
| 2026-09-21 | TASK-0015b-diagnostics-select-and-scatter-sensors | Implemented `TASK-0015b-patch-1` in full (Lead Agent inline, no sub-agents available in this environment), reviewed against every Acceptance Criterion (Phase 4a/4b, inline — Lead Agent as Worker then Reviewer) — **PASS**. `diagnostics/compare_regressions.py`: new `_data_generator(points) -> str` helper (`"return " + json.dumps(points)`); `_pool_series`/`_append_selected_series` now emit `{"name": ..., "data_generator": ...}` (no `data` key) for every series entry, slot-pool and selected-prediction/selected-actual alike, `sensor_id="sum"` included (shared code path, no separate branch needed). `sensor.py`: `ShadyDiagnosticsSensor.extra_state_attributes` now rebuilds each `series` entry as `{"entity": self.entity_id, "name": ..., "data_generator": ...}` before returning — the one field `diagnostics/` cannot supply itself; `accuracy` and every other attribute key pass through untouched. `tests/test_diagnostics_compare_regressions.py` updated to assert `data_generator` (reusing the production `_data_generator` helper itself for expected values, avoiding a duplicated/driftable format string); `tests/test_sensor_diagnostics.py` gained two new tests confirming the `entity`-injection step, including that two sensors sharing one cached `compute()` result each stamp their own `entity_id`. Full suite green: `pytest` 637 passed (635 + 2 new test methods — every other change to `test_diagnostics_compare_regressions.py` was a rewritten assertion, net zero new tests there), `mypy --strict` clean (59 files), `ruff check`/`ruff format --check` clean (180 files), `mdformat --check` clean on every edited/created `.md` file. No new external dependency (`json` is stdlib; `tasks/DEPENDENCIES.md` unchanged). `Status` → `done`. | Routine Phase 4c close-out, immediately following the Phase 0 amendment pass above in the same session. |
| 2026-09-21 | TASK-0015b-patch-1-apexcharts-card-series-format | Created `TASK-0015b-patch-2` (Phase 6 Scenario C, chained off patch-1 rather than off the original task — dependency `[TASK-0015b-patch-1-apexcharts-card-series-format]`): the human reported, same day, that `apexcharts-card` cannot render a numeric x-axis at all — not a `data_generator`-phrasing problem, a hard capability gap in the card patch-1's shape targeted, discovered only once actually tried against a running dashboard. The human found `custom:plotly-graph` supports it, and confirmed a concrete shape live: `{"entity": "", "name": ..., "type": "scatter", "mode": "markers", "x": [...], "y": [...]}` — `x`/`y` as parallel flat arrays, and `entity` now a **constant empty string**, not a self-reference (contrast patch-1, where `entity` was `self.entity_id` — the human's example spelled this out explicitly, so no fork needed asking this round). Because `entity` is no longer runtime-dependent, `diagnostics/` can build the *entire* entry itself — `sensor.py`'s patch-1 `entity`-injection step has no remaining reason to exist. Amended ADR-004 *before* implementation, per Phase 0: §2's canonical example rewritten to the new shape (kept-current-in-place, not layered); §2c marked superseded-same-day rather than deleted (a real rejected step worth a reader knowing about — contrast fully-abandoned pre-implementation sketches noted elsewhere in this ADR's own header, which *are* deleted); new §2d carries the full Reason/Decision for the `plotly-graph` shape; §5's entity-injection paragraph reverted to (once again, truthfully) describe pure pass-through, with a short parenthetical on the two-step same-day history rather than a second full amendment block. `tasks/adr-summary.md` updated to match. | Same trigger as patch-1's own creation (Scenario C) — a `done` task's interface found insufficient by a downstream consumer (here, the human's own dashboard) after the fact. Chained onto patch-1 rather than the original task since patch-1's specific interface, not `TASK-0015b`'s broader one, is what's being superseded. |
| 2026-09-21 | TASK-0015b-patch-1-apexcharts-card-series-format | The human separately flagged, while ADR-004 §2d was already in progress, a YAML correctness issue in the very shape being documented: the `"y"` key must stay explicitly quoted in any hand-written YAML rendering of this shape, because YAML 1.1 (the schema in play wherever the dashboard template's rendered `entities:` text is actually parsed) resolves bare `y`/`Y`/`yes`/`n`/`N`/`no`/etc. to booleans — an unquoted `y:` is the boolean key `True`, not the string `"y"` `plotly-graph` expects. Concretely confirmed live in this session: `mdformat` (this repo's own markdown-formatting tool, run as part of every `.md` edit's own Definition of Done) had already silently stripped that exact quoting from ADR-004 §2's first ```` ```yaml ```` example the moment it was formatted — the bug the human was warning about had already happened once, inside this very task, before being caught. Fix: re-quoted `"y"` throughout §2/§2d's examples, fenced both as ```` ```yml ```` rather than ```` ```yaml ```` (`mdformat`'s YAML-reformatting pass only targets the `yaml` tag, confirmed by direct test against a throwaway file before relying on it), and added an explicit named-constraint paragraph in §2d — not just correctly-quoted examples, but a callout explaining *why*, so a future edit doesn't "clean up" the quotes back out under the same mistaken assumption `mdformat` itself made. `tasks/adr-summary.md` updated with the same callout. | Not a new patch task — no code was affected (the Python `dict`/`str()` path was never at risk; `str()` always quotes string dict keys regardless of content), only documentation correctness for hand-written YAML, caught and fixed within the same `TASK-0015b-patch-2` documentation pass already underway. Logged here rather than folded silently into the patch-2 entry above because it is a distinct, non-obvious finding (a formatting tool actively fighting a security/correctness property of its own output) worth its own audit-trail line. |
| 2026-09-21 | TASK-0015b-patch-1-apexcharts-card-series-format | Implemented `TASK-0015b-patch-2` in full (Lead Agent inline; Worker then Reviewer, inline) — **PASS**. `diagnostics/compare_regressions.py`: removed `_data_generator`/`import json`; new `_xy_series_entry(name, points) -> dict[str, Any]` returning `{"entity": "", "name": name, "type": "scatter", "mode": "markers", "x": [p[0] for p in points], "y": [p[1] for p in points]}`; `_pool_series`/`_append_selected_series` build every entry through it, `sensor_id="sum"` included (shared code path, unchanged). `sensor.py`: `ShadyDiagnosticsSensor.extra_state_attributes` reverted to a plain `return result.attributes` — patch-1's `entity`-injection block removed outright, per §2d/§5. `tests/test_diagnostics_compare_regressions.py`: assertions rebuilt around `_xy_series_entry` (imported from the production module, same drift-avoidance approach as patch-1's `_data_generator` reuse). `tests/test_sensor_diagnostics.py`: `_SeriesDiagnosticMode`'s fixture data and the entity-injection test class replaced with `TestDiagnosticsSensorSeriesPassesThroughUnchanged`, asserting `extra_state_attributes` returns `series`/`accuracy` unmodified with no `entity_id` set on the sensor at all (the injection this test used to require no longer exists). Full suite green: `pytest` 636 passed (637 − 2 removed entity-injection tests + 1 net-new pass-through test), `mypy --strict` clean (59 files), `ruff check`/`ruff format --check` clean (181 files), `mdformat --check` clean on every edited/created `.md` file (post-fix, per the finding above). No new external dependency — `json` removed, nothing added; `tasks/DEPENDENCIES.md` unchanged. `Status` → `done`. | Routine Phase 4c close-out, following both the Phase 0 amendment pass and the y-key documentation fix above, same session. |
| 2026-09-21 | TASK-0015b-patch-2-plotly-graph-xy-series-format | Created and implemented `TASK-0015b-patch-3` in one pass (Phase 6 Scenario C, chained off patch-2): direct human request, same day — `_xy_series_entry` (patch-2's helper for building one `series` entry) belongs on `DiagnosticMode` (`base.py`) as a shared, inherited method, not as a private function local to `CompareRegressionsMode`'s own module, since ADR-004 §2/§5 has always described `series`'s shape as belonging to `DiagnosticMode` output in general and ADR-013 already sketches future modes that would need the identical shape. No design fork to put to the human this round — the request was a direct, unambiguous instruction, not a choice between approaches. Amended ADR-004 *before* implementation, per Phase 0: new §2e (Reason/Decision — a `@staticmethod`, not `@abstractmethod`, since every mode wanting this shape wants it identically, nothing to override), header running-amendments summary extended, the `"y"`-quoting callout's module reference corrected (`base.py`, not `compare_regressions.py`). `tasks/adr-summary.md` updated in two places (the `ShadyDiagnosticsSensor` bullet and the `DiagnosticMode` base-class description). Implementation (Lead Agent inline; Worker then Reviewer, inline) — **PASS**: `diagnostics/base.py` gained `DiagnosticMode._xy_series_entry` (`@staticmethod`, byte-identical shape/behavior to patch-2's version) plus a docstring paragraph on why it lives there now; `diagnostics/compare_regressions.py`'s module-level `_xy_series_entry` removed outright, its two call sites (`_pool_series`, `_append_selected_series`) switched to `self._xy_series_entry(...)`; `tests/test_diagnostics_compare_regressions.py`'s helper import repointed to `shady.diagnostics.base`'s `DiagnosticMode._xy_series_entry` — a pure relocation, so no assertion about the *shape* series entries take needed to change anywhere, only the one import line. Full suite green: `pytest` 636 passed (unchanged count — a pure refactor adds and removes nothing), `mypy --strict` clean (59 files), `ruff check`/`ruff format --check` clean (181 files), `mdformat --check` clean on every edited/created `.md` file. No new external dependency; `tasks/DEPENDENCIES.md` unchanged. `Status` → `done`. | A `done` task's interface (patch-2's `series`-entry builder) found insufficient not by a bug, but by scope — a helper that should have been on the shared base class from the start, once a second mode's eventual need became the actual reason to look. Chained onto patch-2 (the task whose delivered artifact is what's moving) rather than the original `TASK-0015b`, same pattern as patch-2 chaining onto patch-1. |
| 2026-09-22 | TASK-0015b-diagnostics-select-and-scatter-sensors | Created and implemented `TASK-0037` in one pass (Phase 6 Scenario C, chained off the original diagnostics task -- no `TASK-0015b-patch-*` in this chain touched `cache.py`, so it isn't a patch on any of those). Reported live, in chat, by the human: the diagnostic scatter sensors' `series` always showed the historical training scatter but never the "selected {method}"/"selected actual" entries, for any auto-tracking (not manually pinned) diagnosed slot. Root-caused by inspection (no uploaded logs, no deployed diagnostic instrumentation this round): `get_pinned_slot_pool` (ADR-007a §6) validates the whole day-range spanned by its window in one call, including -- while auto-tracking -- the as-yet-unlived remainder of "today"; `_validate_range` (§4) never re-queries an index already inside a validated span even if it came back `None` only because it was queried before it had actually happened yet, so the very first call of the day permanently froze every not-yet-elapsed slot at `None` for the rest of the day, and `_selected_value`'s `get_time_range` read of the diagnosed slot's own value shared that same poisoned validated range. Amended ADR-007a §6 before implementation, per Phase 0 (a real behavior change: `get_pinned_slot_pool` gains a `reference` parameter and caps how far it validates, mirroring `diagnosed_slot()`'s own pinned-vs-auto-tracking split). `tasks/adr-summary.md` updated to match. Implemented and verified inline (Lead Agent as Worker then Reviewer) -- **PASS**. Two pre-existing tests initially regressed against a first draft of the cap (an exclusive-only cap incorrectly excluded the exact pinned slot itself in a test that pins to the same instant as its own fake "now") -- resolved by a pinned-vs-auto-tracking split in the cap itself rather than by loosening it uniformly, which would have reintroduced the original bug for auto-tracking one tick later. New coverage proves the fix directly: a not-yet-elapsed slot is dropped, then populated once a later call's clock has advanced past it (fails against the pre-fix code). Full suite green: `pytest` 640 passed, `mypy`/`ruff check`/`ruff format --check` clean. No new external dependency. `Status` -> `done`. | Directly-reported bug, root-caused and fixed in the same session -- same category as `TASK-0034`/`TASK-0034-patch-2`'s own live bug-report-driven entries above, admitted outside the normal Phase 1/2 capability-slicing flow. |
| 2026-09-22 | TASK-0037-pinned-slot-pool-not-yet-elapsed-freeze | Created and implemented `TASK-0037-patch-1` (Phase 6 Scenario C, chained off `TASK-0037`): the human deployed and restarted after `TASK-0037` shipped, and reported the "selected" series still did not appear -- that fix proved necessary but not sufficient. Root-caused this round by direct reproduction (a small, realistic three-day, constant-`FC` test scenario, not a log-based diagnosis): `wls3.fit()` genuinely raised `numpy.linalg.LinAlgError: Singular matrix`, uncaught, while adding test coverage for `TASK-0037`'s own per-string isolation. Two compounding problems: (1) `regression/base.py`'s `fit_weighted_polynomial` added a *fixed* ridge term (`1e-8`) to its batched normal-equations matrix, negligible -- and therefore ineffective -- once `wls2`/`wls3`'s own `FC^2`/`FC^3` design columns push the matrix's entries well past `O(1)` for any ordinary real-world `FC`, at which scale a training window with little `FC` variety (plausible, not pathological) is genuinely rank-deficient; (2) neither `coordinator.py`'s `_diagnostics_tick_sync` nor `compare_regressions.py`'s `extra_fit()` had any try/except around this fitting call -- unlike `_refit_sync`'s own per-string isolation (ADR-000 §8) -- so the exception silently prevented every string's predictions from ever being cached, repeating on every tick for as long as the underlying (ordinary) data persisted. Amended ADR-008 §1 before implementation, per Phase 0 (a real fix to a documented batched-`numpy.linalg.solve` design: one singular slot aborts the whole batch, confirmed directly). `tasks/adr-summary.md` updated to match. Implemented and verified inline (Lead Agent as Worker then Reviewer) -- **PASS**: `regression/base.py` gained a `_ridge_term` helper scaling the ridge to each slot's own matrix magnitude (falling back to the original constant for the cold-start all-zero-weight case); `extra_fit()`'s per-string loop and `_diagnostics_tick_sync`'s `extra_fit()`/`compute()` calls each gained a `try`/`except Exception`, logged and swallowed rather than propagated. New coverage reproduces the original `LinAlgError` directly and confirms both new try/except layers swallow an injected exception without losing the other string's/tick's own result. Full suite green: `pytest` 645 passed, `mypy`/`ruff check`/`ruff format --check` clean. No new external dependency. `Status` -> `done`. | A `done` task's fix proving necessary but not sufficient, discovered by the same human immediately after deploying it -- Phase 6 Scenario C, same category as `TASK-0034-patch-1`/`-2`'s own downstream-insufficiency entries, chained onto `TASK-0037` rather than treated as a fresh, unrelated bug. |
| 2026-09-22 | TASK-0037-patch-1-ridge-regularization-and-exception-isolation | Created and implemented `TASK-0037-patch-2` (Phase 6 Scenario C, chained off patch-1): the human, live in chat, shared their deployed `compare_regressions.py` after `TASK-0037-patch-1` — the "selected" series still did not appear, now with the key new hint that `accuracy` was an empty `{}` too, meaning `_append_selected_series`'s `fc_selected is None` early return was still firing. Root-caused by re-reading `cache.py`'s own `_validate_range` contract (ADR-012 §2a): `allow_historical_backfill` was, until this task, exclusive to `get_regression_pools` — neither `get_time_range` (`_selected_value`/`target_cell_temperature_for_slot`) nor `get_pinned_slot_pool` ever passed it, and `get_regression_pools`'s own window stops at yesterday by design, so a push-sourced baseline's already-elapsed history for *today* was never fetched by any caller at all. Amended ADR-007a §4 before implementation, per Phase 0. `tasks/adr-summary.md` left as-is (no summarized signature drifted enough to need an update beyond §4's own ADR text). Implemented and verified inline (Lead Agent as Worker then Reviewer) — **PASS**: `get_time_range` (all three overloads) gained a keyword-only `allow_historical_backfill: bool = False`; `get_pinned_slot_pool` now always passes `True`; `_selected_value` and `target_cell_temperature_for_slot` both opted in. New coverage in `tests/test_cache_core.py` (`TestGetTimeRangeThreadsAllowHistoricalBackfill`) and `tests/test_diagnostics_compare_regressions.py` (`TestSelectedValuePassesAllowHistoricalBackfill`). Full suite green: `pytest` 648 passed, `mypy`/`ruff check`/`ruff format --check` clean. No new external dependency. `Status` → `done`. **Backfilled into this log by the following session** (the task file itself already recorded `Status: done`, staged in git but this table/log row was never written before the session ended) — reconstructed from `tasks/TASK-0037-patch-2-allow-historical-backfill-for-get-time-range.md`'s own Goal/Delivered Artifacts/Definition of Done, not re-verified against a fresh diff. | A `done` task's fix proving necessary but not sufficient yet again — same category as `TASK-0037-patch-1`'s own trigger, chained onto patch-1 rather than `TASK-0037` directly since patch-1's own fix, not the original task's, is what's being extended. |
| 2026-09-23 | TASK-0037-patch-2-allow-historical-backfill-for-get-time-range | Created and implemented `TASK-0037-patch-3` (Phase 6 Scenario C, chained off patch-2): the human shared a fourth-round finding, live in chat, this time not a symptom description but a concrete, self-diagnosed correlation — "the diagnostic sensors contain the selected series only if the evaluated slot is the first slot of the hour (matches minute 0)". Root-caused by direct reproduction against the real coordinator (not by reading alone, though reading narrowed the search first): `_push_provider_series` (`coordinator.py`, ADR-012 §4) built its push `dict` as a plain `{Cache.index_for(ts): value for ts, value in series}` — a 1:1 timestamp match against `provider.forward()`'s own raw series. For a coarser-than-5-minute source (`forecast_solar`/`weather_sunshine`/`weather_cloud` — always hourly, ADR-009 §1a), that only ever writes the exact slot each raw sample lands on; every other 5-minute slot in that sample's span was never written at all and stayed permanently `None` (`to_index=None` once pushed, so `_validate_range` never re-queries it later). `_recompute_string`'s own sibling read of the identical `forward()` series already solves this exact problem via `_forward_fill_by_day` (ADR-009 §1a) — `_push_provider_series` never got the same treatment. Confirmed directly: pushed a synthetic hourly `forward()` series through the real, unmodified `_push_provider_series` and read the cache back at every 5-minute offset — `None` at every slot except the two hour marks bracketing the range, reproducing the report exactly. Also explains why the existing suite never caught it: every test in `test_diagnostics_compare_regressions.py` pins its diagnosed slot to `_PIN = datetime(2026, 6, 10, 10, 0, ...)` — minute 0 — so the gap was never exercised. Amended ADR-012 §4 before implementation, per Phase 0 (a real behavior change to the generic push loop's conversion step, extending ADR-009 §1a's already-established forward-fill rationale to a second call site rather than reimplementing it there). `tasks/adr-summary.md` §4's push-loop description updated to match. Implemented and verified inline (Lead Agent as Worker then Reviewer) — **PASS**: `_push_provider_series` now runs `series` through `_forward_fill_by_day(series, now, _tomorrow_end(now))` (same horizon `_recompute_string` already uses — `pin_diagnostic_slot` itself already rejects any pin at or past it, so nothing legitimate reads a pushed slot beyond it) before building the push `dict`, reusing the existing helper rather than adding a second one. New coverage: `tests/test_coordinator.py`'s `TestGenericPushForwardFillsCoarserGrid` (an hourly `forward()` series fills every 5-minute slot in between; a value change between two raw samples still steps at the second sample's own timestamp, not interpolated) and `tests/test_diagnostics_compare_regressions.py`'s `TestFuturePinnedSlotSelectedResolvesOffHourAlignment` (a quarter-past-the-hour future pin against an hourly-only pushed baseline now shows `"selected method_x"`) — all three confirmed to fail against the pre-fix `_push_provider_series` before being confirmed to pass against the fix. Full suite green: `pytest` 653 passed (650 + 3 new), `mypy --strict` clean, `ruff check`/`ruff format --check` clean, `mdformat --check` clean on every edited `.md` file (one identifier accidentally split across a code span by manual line-wrapping, caught and fixed by `mdformat --check` failing before this row was written). No new external dependency. `Status` → `done`. Also backfilled the previous session's missing `TASK-0037-patch-2` table row and log entry (row above) as part of this pass, per the Refinement Guard's "no structural change without a log entry" check. | Directly-reported, self-diagnosed correlation from the human — root-caused by reproduction against the real code path rather than assumed from the correlation alone, and distinguished explicitly from `TASK-0037`/`-patch-1`/`-patch-2`'s own root causes (validation timing, fitting exceptions, missing backfill opt-in respectively) before being treated as a new patch rather than folded into one of those. |
| 2026-09-23 | TASK-0037-patch-3-forward-fill-pushed-baseline-series | The human ran `mypy` for real (`tests\test_coordinator.py:710`/`736`, `tests\test_diagnostics_compare_regressions.py:459` — Windows paths, a different machine than this session's own) and reported three `Unused "type: ignore" comment [unused-ignore]` errors, none of which this task's own Definition-of-Done check had caught: that check ran `mypy` against `custom_components/shady/coordinator.py` alone, never against the two edited test files, so the claim "`mypy --strict` clean" in both this row above and the task file's own Definition of Done was wrong the moment it was written. Root cause, confirmed by reproducing the same errors locally: the three new tests' `provider.forward = lambda now: hourly_series` reassignments each carried a `# type: ignore[method-assign]`, copied by habit from the same pattern in production code (where `provider` is a properly-typed `Provider` and the ignore is genuinely required) — but in these test files `provider` traces back to a dynamically-`_load`ed class, typed `Any`, so mypy never raises `method-assign` there and `warn_unused_ignores = True` (`mypy.ini`'s project-wide default, deliberately *not* overridden for test files the way it is for the handful of HA-subclassing production modules) correctly flagged the dead comments. Fixed by removing all three; reverified with the project's own real invocation, `mypy --config-file mypy.ini custom_components/ tests/` (61 files, clean) rather than a single-file check — and `ruff check .`/`ruff format --check .` run across the whole tree too this time (a pre-existing, unrelated `EXE002` "executable but no shebang" finding on six untouched test files, permission bits already set before this session started, left alone as out of this patch's scope). Corrected the same wording in `tasks/TASK-0037-patch-3-forward-fill-pushed-baseline-series.md`'s Definition of Done and Delivered Artifacts (the latter also gained a note on why the ignores were unnecessary, so a future edit doesn't re-add them from the same habit). No ADR amendment, no new patch task — a verification gap in this task's own close-out, corrected in place before any downstream task consumed it, same category as `TASK-0034`'s own "Corrected a factual assumption... same session" entry and patch-2's own "y-key quoting" documentation-correctness entry. `Status` remains `done` throughout; `pytest` still 653 passed, `mdformat --check` still clean, no new external dependency. | This task's Definition of Done said "the full test suite passes" and "`mypy`/`ruff check`/`ruff format --check` clean on every edited file" but the verification step actually run didn't cover every edited file — caught by the human, not by this session's own process, which is itself the reason to log it explicitly rather than silently amend the earlier row: a Definition of Done is only as good as what was actually checked against it. |
| 2026-09-23 | TASK-0037-patch-3-forward-fill-pushed-baseline-series | The human pointed out test coverage had dropped slightly on both touched production files and asked for additional coverage of non-defensive branches, plus flipping both modules' debug-only `_DIAGNOSTIC_LOG` flag on for the whole test run while touching these files anyway. Measured directly (`coverage run --branch --source=custom_components/shady`) rather than guessed: before this pass, `coordinator.py` was 96% (845 statements, 21 missing) and `compare_regressions.py` 94% (169 statements, 4 missing). Flipping `_DIAGNOSTIC_LOG = True` in `tests/test_coordinator.py`'s top-level module-load section (both `_coordinator_mod` and `_compare_regressions_mod`, reused by every file that imports it — one place suffices for the whole suite) immediately closed most of the gap: every `if _DIAGNOSTIC_LOG:` body across both modules now executes at least once via the suite's own existing, otherwise-unrelated calls into `_fit_string`/`_diagnostics_tick_sync`/`extra_fit`/`compute_sensor`/`_selected_value`, none of them raising -- confirming the debug logging itself (five call sites, several building dict/numpy lookups a wrong key or shape could break) is now safe to actually turn on in production, the entire reason the flag exists. Audited every remaining missing line/branch in both files by hand and classified each: per-string/per-tick `except Exception` isolation catches (ADR-000 §8, defensive, several already covered elsewhere) and the `_DIAGNOSTIC_LOG` flag-check's own now-permanently-one-sided branches were left alone; `extra_fit()`'s `if predictions:` guard confirmed statically unreachable (`REGRESSION_STRATEGIES` is a fixed non-empty constant) and left alone; six genuinely non-defensive, previously-untested branches got new tests -- `target_cell_temperature_for_slot`'s two baseline-unresolved paths, `_handle_actual_yield_update`'s `pv_sum() is None` path, `extra_fit()`'s `_selected_value`-resolves-to-`None` skip (distinct from, and previously conflated with, the already-covered `baseline_entity_id is None` skip), `_append_selected_series`'s own `fc_selected is None` early return (the original `TASK-0037` report's exact failure mode, confirmed still graceful), and `_predict_all_methods`'s `target_cell_temperature_for_slot`-returns-`None` path. A remaining cluster -- energy-reset day-boundary no-ops, intraday-correction empty-push, a `NaN`-output skip inside the reverse-transform pipeline, several sequential early-returns inside the startup-only `_backfill_elapsed_today_slots_for_string`, and one async Forecast.Solar service-call fallback -- was deliberately left uncovered: each is pre-existing, unrelated to `_push_provider_series`, and would need its own non-trivial scenario in a subsystem this patch never touches; logged here as a known gap rather than silently dropped. Two mypy errors surfaced while adding the new tests, both fixed the same pass: `_coordinator_mod._DIAGNOSTIC_LOG = True`/`_compare_regressions_mod._DIAGNOSTIC_LOG = True` each needed `# type: ignore[attr-defined]` (these two module objects are properly `ModuleType`-typed, unlike the class instances loaded through them, so mypy does check the attribute -- the opposite lesson from the previous row's `method-assign` correction, both now recorded so neither mistake repeats); a monkeypatched `_selected_value` replacement returning `Any` from a function declared \`-> float | None`was fixed by declaring it`-> Any`instead of stripping the annotation entirely (a bare stripped annotation trips`disallow_untyped_defs`in this project's`mypy.ini`). Final state: `pytest`659 passed (653 + 6 new),`coordinator.py`98% (13 missing, all classified above),`compare_regressions.py`98% (0 missing statements, only classified branches remain),`mypy --config-file mypy.ini custom_components/ tests/`/`ruff check .`/`ruff format --check .`all clean,`mdformat --check`clean on every edited`.md`file. No ADR amendment (no production behavior changed beyond the flag flip, which is test-only), no new patch task -- coverage/test-quality follow-through on this same task's own delivery, same category as the two correction rows above it.`Status`remains`done\` throughout; no new external dependency. |
| 2026-09-24 | TASK-0037-patch-3-forward-fill-pushed-baseline-series | Created and implemented `TASK-0037-patch-4` (Phase 6 Scenario C, chained off patch-3, the latest task in the diagnostic-module rework): the human found the companion clear button (`ShadyClearDiagnosticSlotButton`, ADR-004 §2f) "not very helpful" and specified its replacement — a toggle between one pinned slot and automatic slot following, where automatic mode *sets* the pinned slot on every cadence (so the `datetime` entity's own state is the dashboard's "as of" timestamp) and diagnostics always read the currently configured slot, pinned or automatic. Recorded honestly: the button was introduced in the same commit as `TASK-0037-patch-1`/`-2`/`-3` but never had a task file or INDEX row of its own — only ADR-004 §2f — so there was no earlier task to chain onto directly. Amended ADR-004 *before* implementation, per Phase 0 (new §2g; §2f marked superseded in part rather than deleted, following §2c's precedent; §2a/§5/header updated to match), plus ADR-000 §2/§3 (which had also never listed `datetime.py`), ADR-007a §6, `adr/INDEX.md`, `tasks/adr-summary.md`, `tasks/adr-capability.md`. Design details chosen by the Lead Agent and flagged to the human for confirmation rather than asked up front, since the request was otherwise unambiguous: the switch is `on` = following; turning it off pins the slot as currently shown; setting the datetime while following pins and turns following off; "every cadence" = the existing 5-minute tick, run first in `_intraday_tick_sync` and independent of whether a diagnostic mode is active; `cache.pinned_reference` stays unset while following so ADR-007a §6's pinned-vs-auto-tracking cap is untouched; no push mechanism (entities keep default polling); not persisted across restarts; no registry migration for the removed button. Implemented and verified inline (Lead Agent as Worker then Reviewer) — **PASS**: `coordinator.py` replaced `_pinned_slot_index` with an always-set `_diagnostic_slot_index` plus `_follow_latest_diagnostic_slot`, `diagnosed_slot()` now reads the stored index unconditionally, new `set_follow_latest_diagnostic_slot`/`is_following_latest_diagnostic_slot`/`diagnostic_slot_timestamp`/`_advance_followed_diagnostic_slot`, removed `clear_diagnostic_slot`/`pinned_diagnostic_slot`; new `switch.py`; `datetime.py`'s `native_value` never `None`; `button.py` reduced to `ShadyRecalculateButton`; `PLATFORMS` gained `switch`. Full suite green: `pytest` 683 passed (659 baseline + 24 net: `tests/test_switch.py` 10 new, coordinator/datetime/init additions, minus the removed clear-button tests), `mypy --config-file mypy.ini custom_components/ tests/` clean (63 files), `ruff check .`/`ruff format --check .` clean. No new external dependency. `Status` → `done`. | A `done` task's delivered interface (the diagnosed-slot pin/clear surface) found insufficient by the human in use, after the fact — Phase 6 Scenario C's own trigger, same as `TASK-0037-patch-1`/`-2`/`-3`. Original tasks' `Status` untouched. |

## Audit Groups

Not applicable yet for this cycle — Phase 7 triggers once every task in this
cycle's own table is `done` and the human asks for a fresh audit pass. With only
one task in this cycle so far, a dedicated audit batch is not warranted on its
own; folding this change into whatever group `providers/discovery.py`/
`coordinator.py` land in during a future round-3 audit (should one be requested)
is the more proportionate path, consistent with how round 2 scoped itself to
only the files that actually changed since round 1.
